#include <iostream>
#include <unistd.h>
#include <stdlib.h>

int main() {
    long page_size = sysconf(_SC_PAGE_SIZE);
    std::cout << "Page size: " << page_size << " bytes" << std::endl;

    return 0;
}
